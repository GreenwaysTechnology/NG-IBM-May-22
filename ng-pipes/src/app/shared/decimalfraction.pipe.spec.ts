import { DecimalfractionPipe } from './decimalfraction.pipe';

describe('DecimalfractionPipe', () => {
  it('create an instance', () => {
    const pipe = new DecimalfractionPipe();
    expect(pipe).toBeTruthy();
  });
});
